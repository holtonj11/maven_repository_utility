# Maven Repository Scraper - Dependencies
# Core dependencies
requests>=2.28.0
urllib3>=1.26.0

# Optional: For proper XSD validation (recommended)
# If not installed, falls back to simple XML validation
lxml>=4.9.0

# For progress bars (optional)
# tqdm>=4.64.0

# For concurrent downloads (built-in in Python 3.11+)
# concurrent.futures is part of standard library