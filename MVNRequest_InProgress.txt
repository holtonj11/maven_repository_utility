Please write a Python script that performs the following:

- Generate a Python script that performs the following:
     - Scrapes Maven release libraries from the following, configurable repository list (should be able to add new repository links, thus, should be an array or list):
          - https://repository.mulesoft.org/nexus/#browse/browse:releases
               The above terminates in a URL like the following: https://repository.mulesoft.org/nexus/#browse/browse:releases:com%2Fanypoint%2Fjava%2Fclients%2Fapi_designer%2F0.3
               com%2Fanypoint%2Fjava%2Fclients%2Fapi_designer%2F0.3 represents the com.anypoint.java.clients.api_designer library, where each part of this string represents a directory name, with the dots being path delimiters, and the 0.3 being the library's version.  I would like https://repository.mulesoft.org/nexus/#browse/browse:releases to be fully pulled into a configurable directory path, which by default will be user/.m2/repository (where "user" is the Windows user.  Assume we don't know the Windows user name, is user agnostic).
          - https://repo1.maven.org/maven2/
               The above repository is easier to navigate then the first one I provided.  The reason I'm providing multiple and would like to be able to add more as we go is one may have some libraries the other doesn't have.  As many of the libraries may be duplicative, the script should be able to detect this so we only pull each library once.  We are interested in pulling all files inside each deepest directory and use the .pom files to find all parent and transitive dependencies, and each of their parent and transitive dependencies n number of levels until we've recursed the entire repository.  
     - Creates a fully recursive Maven dependency tree
          - Store a Maven dependency tree of the now local Maven repository created above in a configurable subdirectory, defaulting to the directory the script is run from as it's parent.  Name this directory "directoryTree_output.  Files should be named dependencyTree_timestamp.txt where timestamp is in form yyyy-mm-ddTHH:mm:ss.FFF to ensure uniqueness, and dependencyTree_timestamp.json.  The JSON file should have a schema like:
               {
                     "library": library Maven path (like com.anypoint.java.clients.api_designer.0.3 for instance), 
                     "version": in the example given in the "library" attribute above, this would be "0.3",
                     "filePath": file path of local library, for instance for the library above in the "library" attribute for a Windows username Jon "C:\\Users\\Jon\\com\\anypoint\\java\\clients\\api_designer\\0.3",
                     "issues": [] (populate this array with issue strings that will be described later in this request)
                     "error": "Error message if not handled in "issues" attribute,
                     "parentLibrary": The parent library,
                     "transitiveLibraries": [] Populate this array with all transitive libraries of this library,
                     "parentLibraryPath": full path of parent, in order from root to this particular library, maybe separated by "->"
               }
               - Issues above in the attribute "issues" can include the following:
	            - .pom file is missing.  Value of this issue should be "POM file is missing"
		    - .jar file is missing.  This isn't necessarily an error condition, it could be perfectly legitimate to not have a JAR file present.  If the POM file is missing and the JAR file is also missing, then value of this issue should be "JAR file and POM file are missing" or if .pom file is not missing and .jar file is missing, then "JAR file is missing but POM file present"
		    - .pom file exists but contains HTML content instead of XML content.  In these examples, the first line will contain either "<html" or "<!DOCTYPE html" (leaving the tab open in my examples is intentional). Value of this issue should be "HTML only content"
   		    - .pom file exists, is not HTML content like the above check, but the content isn't valid XML.  There are several possible checks that can be performed to determine if the .pom file is valid XML, we can simply check that the first line contains "<?xml" (again, I left the tag open on purpose), or we can validate against an xsd schema.  I wish to be able to provide arguments for the following:
                         - Simple XML check (again, check that first line contains "<?xml"
                         - Validate against XSD schema.  First check a configurable (via argument) directory where the XSD file is stored.  Let's call this file maven-4.0.0.xsd by default, but make this name configurable also by argument (in case we want to keep multiple versions).  By default, assume this directory to be the directory the script is run from.  Failing that, check the following URL for the schema:
                              https://maven.apache.org/xsd/maven-4.0.0.xsd
                           If local XSD file does not exist, download from this location.  Also, make this URL configurable via argument in case of later newer versions or URL changes.  Allow via argument for the toggling between simple and XSD XML validation, but assume by default we want XSD validation.  The value for this issue should be either "Failed simple XML validation" or "Failed XML XSD schema validation" depending on which is enabled.  
                     - .jar file is less than 5 KB.  This indicates that the JAR file is corrupt.  The value for this issue should be "JAR file is invalid"
		     - .pom file is proper XML, given the XML checks defined above, but .pom file does not contain anywhere a "groupId", "artifactId", or "version" configured.  This can be assessed simply by seeing if these xml tags all exist.  These can be anywhere in the XML structure, so use something like namespace and .//groupId, etc...  The value of this issue should be "POM file is not valid Maven"
               If the "POM file is missing" issue is detected, the only other check that should then be performed is the check for the presence of the JAR file, as other checks have already then failed.  If the POM file is in HTML format, there is no need to perform the XML checks, as these checks have already then failed.  If any of these issues occur, attempt to scrape the Maven repository URLs again to replace the problem file (for instance, if a POM or JAR are missing, check if it exists at the repo and download it to the local library directory.  If the POM file is invalid, such as is HTML content or not valid XML, attempt to download it again to the library's local directory.  Don't prompt to overwrite the file, just do it.  Then, check it again, exactly once (don't keep checking and end up in an endless loop).  If the problem persists, record the issue as normal and move on.  If any errors not mentioned above occur, handle the error and also record it in the "issues" array.  Continue executing the script despite the error or any other errors if possible.  Also, if a library exists at one of the repository URLs but doesn't exist in our local repository, please download it.  The goal is to always maintain an up to date, full repository.  You may need to somehow record libraries you have already downloaded and validated (again, using the rules above).  

Use Maven's rules (not file system rules, but use the parent tags and such that Maven POM files provide) for determining parent dependencies and transtive dependencies to n levels.  I want a fully recursive dependency tree, and always a full, up to date repository of all known libraries.  I want this script to be heavily configurable, have full error handling, and have full logging both to file and console.  The goal is to make the most comprehensive Maven repository scraper and maintenance tool possible.  Also, in addition to text and JSON dependency trees being output, with the JSON file containing the issues array, I would additionally like each distinct issue separated into files that contain only that issue.  For instance, having a file with the text of the issue appended to the filename, and only containing libraries with full parent and transitive dependencies of those libraries, for instance, "Failed simple XML validation" could additionally have a file that contains only those libraries and their dependencies that meet this condition, would be a nice bonus.  Also, for the text file, please arrange the levels using something like 
     |__   
This would give us sort of an easy to read ladder format.  I currently can't think of much more to add to this request, but please make suggestions if you feel I've missed a detail or there's an improvement you can suggest.  Thank you.
              